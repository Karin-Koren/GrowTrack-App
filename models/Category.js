export class Category {
  constructor(id, title, budget, balance, available) {
    this.id = id;
    this.title = title;
    this.budget = budget;
    this.balance = balance;
    this.available = available;
  }
}
